package Suppliers.BusinessLayer;

public enum Status {
    // enum description: this enum describes all the possible status' for orders: inprocess - not delivered yet,
    // completed - delivered successfully, canceled - order was canceled before arrival.
    InProcess, Completed, Canceled}
